import { gmC } from "../helpers/constants";

const initialState = {
  users: [],
  messages: [],
};

export const gameReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
